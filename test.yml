config:
  target: "http://localhost:3000"
  phases:
    - duration: 10        # run the test for 60 seconds
      arrivalRate: 1000    # ~167 new users per second (167 * 60 ≈ 10,000)
  engines:
    socketio: {}

scenarios:
  - engine: socketio
    flow:
      - emit:
          channel: "join"
          data: "user-{{ $randomNumber(1, 1000000) }}"
      - emit:
          channel: "chat message"
          data: "Hello, testing load!"
